export type TypeServiceFile = {
  data: {
    serviceBy: {
      servicesContent: {
        serviceFile: {
          node: {
            mediaItemUrl: string;
          };
        };
      };
      databaseId: number;
    };
  };
};
